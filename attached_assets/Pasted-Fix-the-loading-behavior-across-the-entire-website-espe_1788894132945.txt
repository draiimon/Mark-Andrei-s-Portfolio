Fix the loading behavior across the entire website, especially on mobile devices and slower phones. Right now, some pages become visible before all required content, styles, images, fonts, animations, API data, and important components are fully ready, which makes the website look incomplete or partially loaded. I want a proper full-site loading system that applies consistently to every page and route. Do not show the actual page until the essential content required for that page is fully initialized and visually ready. While the page is still preparing, display a clean, premium loading screen that matches the website’s current dark minimalist design. The loading screen must be controlled by real application state, not by a fake timeout. It should wait for critical fonts, above-the-fold images, required styles, page-level components, essential API/database data, and any assets needed to prevent broken layouts or missing content. Once these critical resources are ready, smoothly transition from the loading screen into the completed page. Apply this logic globally so navigation between Home, About, Projects, Featured Work, Edit pages, Preview pages, chatbot-related pages, and every other route cannot briefly show unfinished UI. For slow devices or poor connections, keep the loading screen visible until the critical page state is actually ready instead of exposing partially rendered sections. Secondary and non-critical assets may continue lazy-loading in the background after the page becomes usable, but anything that affects the visible layout, spacing, typography, hero section, navigation, major images, or core functionality must be ready before revealing the page. Prevent flashes of unstyled content, missing fonts, empty image containers, half-rendered sections, skeletons suddenly changing size, hydration mismatches, layout shifts, and components popping into place after the page is already visible. Use a centralized global loading manager or route-level loading architecture instead of adding random independent loaders everywhere. Track actual loading states from images, fonts, critical data requests, authentication state, required database/API responses, and route transitions. Make sure loaders correctly start and finish during first page load, refresh, direct URL access, and client-side navigation. Add fallback and error handling so the loading screen can never become permanently stuck if one optional asset or request fails. Critical failures should show an appropriate error or retry state, while non-critical failures should allow the website to continue loading normally. Avoid waiting for unnecessary resources such as analytics, off-screen images, below-the-fold content, optional animations, or decorative effects before displaying the page. Prioritize only the resources needed for the first visible screen and essential page functionality. Once the page is shown, progressively load the remaining content in the background. On repeat visits, use cached assets so the loading screen becomes much shorter or may not appear at all if everything required is already available. Make sure this works properly with the existing caching, Service Worker, offline support, database integration, backend APIs, edit system, preview functionality, navigation, and chatbot without removing or changing any existing feature. The final behavior should be simple: if the page is not visually and functionally ready, keep the branded loading screen visible; once the critical page content is genuinely ready, reveal the complete page smoothly. Apply this consistently to the entire website, not just the homepage.
