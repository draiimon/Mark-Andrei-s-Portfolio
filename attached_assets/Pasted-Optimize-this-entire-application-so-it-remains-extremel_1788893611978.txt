Optimize this entire application so it remains extremely lightweight, smooth, fast, responsive, and reliable even on very low-end Android phones, older iPhones, devices with weak CPUs, limited RAM, older GPUs, limited storage, outdated browsers, and slow or unstable mobile connections, while preserving the exact same visual quality, branding, layout, features, animations, images, interactions, and overall premium feel of the website. Do not downgrade the design, remove important effects, reduce image quality unnecessarily, or simplify the UI just to improve performance; instead, improve how everything is implemented, loaded, rendered, cached, and executed behind the scenes.

Audit the full frontend and backend first and identify performance bottlenecks before changing anything. Reduce unnecessary JavaScript execution, remove dead code and unused dependencies, eliminate duplicate logic, minimize client-side hydration where possible, use route-level and component-level code splitting, dynamic imports, lazy loading, tree shaking, and load only the code required for the current screen. Optimize React or Next.js rendering by preventing unnecessary re-renders, avoiding excessive state updates, reducing oversized client components, keeping static content server-rendered where appropriate, memoizing only when it provides measurable benefits, and avoiding expensive work during scrolling, pointer movement, animation frames, or page transitions.

Build an adaptive loading system specifically for extremely weak or old devices. Detect performance capability carefully using practical signals such as available browser APIs, network conditions, reduced-motion preferences, approximate device memory where supported, CPU/rendering performance observations, and connection quality, without relying on fragile device-name detection. If a device is extremely slow, do not attempt to initialize every feature simultaneously. Show a polished branded loading or preparation screen when genuinely necessary while the application prepares the essential resources required for a smooth experience. The loading screen should feel intentional and premium, match the existing portfolio aesthetic, display meaningful loading progress when technically possible, and never appear longer than necessary on capable devices.

For very weak devices, implement staged initialization. First load the minimum application shell, navigation, typography, critical CSS, above-the-fold images, essential UI, and core functionality. Once the interface becomes usable, initialize secondary animations, chatbot resources, additional sections, previews, non-critical images, analytics, background effects, and other heavier functionality progressively. Expensive components should never block the user from seeing or interacting with the main page.

Add a proper Progressive Web App and offline-first strategy where appropriate. Use a Service Worker with versioned caching so the essential application shell, CSS, JavaScript bundles, fonts, logos, icons, important images, and other safe static assets can remain available after the first successful visit. The website should automatically prepare itself for future offline viewing without requiring the user to manually download every resource. However, do not blindly download the entire website on the first visit because this could make low-end devices slower, waste mobile data, consume excessive storage, or trigger browser cache eviction.

Instead, use intelligent progressive caching. Cache critical resources immediately, then cache secondary resources gradually in the background after the main interface is fully interactive. Prioritize the sections and assets the user is most likely to revisit. Allow larger optional media or rarely visited resources to be downloaded only when necessary. Use cache strategies such as cache-first for immutable static assets, stale-while-revalidate for appropriate reusable content, and network-first for information that must remain current. All caches must be versioned and safely invalidated when a new deployment is released so users never become permanently stuck on outdated code.

Make as much of the website viewable offline as technically possible. Previously loaded portfolio pages, project descriptions, images, static content, navigation, layouts, and other cached content should remain accessible even when there is no internet connection. Provide a polished offline state rather than showing browser errors or broken components. Clearly distinguish between content that is cached and functionality that genuinely requires an internet connection.

For database-dependent or backend-dependent features, never pretend that the server is available offline. Cached information may remain viewable offline, but operations that require the live database, authentication server, external APIs, chatbot AI, or other remote services should gracefully switch to an offline state. Where safe and appropriate, allow local changes such as drafts or edits to be temporarily stored in IndexedDB or another reliable local storage mechanism and automatically synchronize them with the backend after the connection returns. Prevent duplicate submissions or conflicting updates during synchronization.

If practical, provide a small non-intrusive indicator when the application has finished preparing offline resources, such as “Available offline,” but do not interrupt the user unnecessarily. If the application needs to download a significant amount of optional content for complete offline availability, perform it progressively in the background or provide a deliberate “Make available offline” option rather than secretly consuming large amounts of mobile data.

Handle browser storage intelligently. Respect storage quotas, avoid caching unnecessary duplicate assets, remove obsolete cache versions, prevent unbounded IndexedDB or localStorage growth, and gracefully recover when the browser refuses additional storage. Never allow offline caching to cause crashes or excessive storage consumption on devices with very little free space.

Optimize all images using responsive srcset and sizes, modern formats such as WebP or AVIF where supported, correct dimensions, intelligent compression, lazy loading, CDN delivery, browser caching, and priority loading only for above-the-fold assets while maintaining visually indistinguishable quality. Keep high-quality originals or appropriate high-resolution variants available for displays that require them, but never send oversized images to a small low-resolution phone when a visually identical smaller resource would produce the same result.

Do not preload unnecessary images, videos, fonts, scripts, animations, or page resources. For high-resolution visual assets, consider progressively loading an immediately usable optimized version first and seamlessly replacing it with the full-quality version only when necessary. The user should never experience an obviously blurry or degraded final website; optimization should reduce wasted bytes, not visible quality.

Optimize fonts by loading only the required families, weights, styles, and character subsets, self-hosting or efficiently caching them when appropriate, using font-display: swap, preloading only genuinely critical fonts, and preventing large font files from blocking rendering. Ensure typography remains visually consistent when fonts are still loading.

For animations, preserve the same appearance but rebuild expensive effects using GPU-friendly transform and opacity whenever possible instead of properties that continuously trigger layout, paint, or expensive compositing. Avoid unnecessary continuous animation loops, excessive full-screen blur, massive backdrop filters, giant shadows, expensive particle systems, and high-frequency pointer or scroll handlers unless they are efficiently implemented.

The custom moon cursor, hover effects, transitions, chatbot animations, galaxy effects, eclipse glow, background movement, glowing elements, interactive project cards, and other portfolio visuals should remain present and visually high quality, but they must adapt intelligently to the performance capability of the device. On strong devices, allow the complete experience. On very weak devices, preserve the same overall appearance while reducing unnecessary computation—for example by lowering internal animation update frequency, pausing invisible animation work, rendering certain decorative effects less frequently, or replacing computationally expensive runtime effects with visually equivalent GPU-accelerated implementations.

Do not simply disable every animation on low-end devices unless absolutely necessary. Prefer adaptive rendering that maintains the design while spending fewer resources. Respect prefers-reduced-motion when the user explicitly requests reduced motion.

Pause animations, videos, observers, timers, polling, WebGL rendering, canvas effects, background processes, and other unnecessary work whenever their elements are outside the viewport, the browser tab is hidden, or the application is not actively being interacted with. Resume them safely when required. Use IntersectionObserver instead of continuously calculating element positions during scrolling.

Avoid expensive mouse-based effects on mobile devices where no mouse exists. The custom cursor system should not even initialize on touch-only devices unless there is a legitimate reason for it. Desktop-only interaction logic should never consume resources on phones.

Use requestAnimationFrame, passive event listeners, throttling, debouncing, and event delegation where appropriate. Move genuinely heavy calculations away from the main UI thread using Web Workers when beneficial. Avoid large synchronous operations that freeze the page. Break unavoidable expensive tasks into smaller pieces so input and scrolling remain responsive.

Prevent memory leaks by correctly cleaning up timers, intervals, event listeners, observers, subscriptions, WebSockets, SSE connections, workers, animation frames, object URLs, temporary media, and large cached JavaScript objects whenever they are no longer needed. Monitor memory behavior during long browsing sessions because low-end phones may terminate tabs aggressively when memory usage becomes excessive.

Keep the DOM size reasonable. Virtualize long lists, activity feeds, project collections, message histories, logs, or other large datasets. Paginate data instead of retrieving everything simultaneously. Never render hundreds of invisible or off-screen elements unnecessarily.

Optimize API communication by preventing duplicate requests, adding request deduplication, caching reusable responses, implementing pagination, batching requests where appropriate, compressing payloads, returning only required fields, optimizing database indexes and queries, reusing connections, avoiding unnecessary polling, and never fetching data that the current page does not need.

For real-time features, avoid wasteful high-frequency polling when WebSockets, Server-Sent Events, event-driven updates, or appropriately spaced refresh intervals would be more efficient. Reconnect intelligently after network interruptions using exponential backoff rather than aggressively retrying.

Preserve the full connection between the UI, backend, database, edit pages, preview functionality, authentication, chatbot, APIs, project pages, forms, navigation, and every existing working feature. Do not break backend contracts or database schemas simply for performance improvements.

Add intelligent browser, server, CDN, and framework-native caching for static assets and reusable data without serving stale critical information. Use immutable hashed static assets where appropriate, compression such as Brotli or gzip, HTTP caching headers, ETags where beneficial, and CDN edge delivery when available.

The application should continue behaving well when the connection changes between Wi-Fi, 4G, weak cellular data, intermittent connectivity, and complete offline mode. Avoid large automatic downloads when the user is on a constrained connection. Where supported, consider signals such as Save-Data or effective network type to postpone non-essential background downloads while still preserving the final visual quality once those assets are actually required.

Design the loading experience intelligently. Do not show a loading screen simply because it looks attractive. On fast devices, render the application immediately. On devices that genuinely require preparation, display the loading interface while essential resources are initialized and cached. The application should never sit on a fake progress bar. Use actual loading state wherever measurable and an indeterminate animation when precise progress cannot be calculated.

If a first-time visitor is using an extremely old or slow phone, prioritize stability over attempting to perform everything simultaneously. It is acceptable for the application to spend slightly longer on an initial preparation/loading screen if doing so results in a significantly smoother session afterward, but this preparation should only download resources that provide real value. Once the necessary assets have been cached, repeat visits should become dramatically faster and should avoid repeating unnecessary initialization or downloads.

Add a resilient startup mechanism. If a secondary script, animation, API request, external font, analytics service, chatbot service, or non-critical component fails to load, the main website should still render and remain usable. Do not allow one optional dependency to prevent the portfolio from opening.

Use progressive enhancement so the core content and navigation remain accessible even when advanced browser capabilities are unavailable. Avoid unnecessary cutting-edge browser APIs unless proper fallbacks exist. Ensure compatibility with older Safari and Chromium versions within a realistic support range.

Keep the initial HTML, CSS, and JavaScript payload as small as reasonably possible. Minimize parser-blocking and render-blocking resources. Load secondary functionality only after the critical interface has become usable. Reduce unnecessary third-party scripts and ensure unavoidable third-party integrations do not block the main thread or initial page rendering.

Prioritize excellent Core Web Vitals, especially fast Largest Contentful Paint, low Interaction to Next Paint, minimal Cumulative Layout Shift, low Total Blocking Time, fast First Contentful Paint, low memory usage, stable scrolling, and consistent animation frame rates.

Test the website under realistic low-end conditions rather than evaluating it only on a powerful development computer. Simulate at least 4× to 6× CPU slowdown, slow 4G, and where appropriate 3G-like network conditions. Test small-screen Android devices, limited-memory situations, older iPhones, Safari, and Chromium-based mobile browsers. Include repeated navigation, long scrolling sessions, opening and closing the chatbot, editing content, previewing pages, switching tabs, reconnecting after losing internet, offline refreshes, and restoring connectivity.

Test both cold loads and repeat visits. The first uncached visit should remain reasonable, while subsequent visits should become significantly faster because of intelligent caching. Also test that a previously loaded version can reopen offline.

Use Lighthouse, Chrome DevTools Performance and Network panels, bundle analysis, React Profiler where applicable, browser memory profiling, Core Web Vitals measurements, coverage analysis, and network throttling to compare the application before and after optimization. Identify long JavaScript tasks, layout thrashing, excessive GPU work, large bundles, duplicate libraries, unused CSS, oversized images, unnecessary requests, memory leaks, rendering bottlenecks, and slow API/database responses.

Do not blindly remove dependencies, features, or visual effects just to obtain a smaller bundle. Every optimization should be safe, deliberate, and measurable. If a visual effect is technically expensive, recreate the same perceived appearance using a more efficient implementation instead of deleting it.

The final result should look essentially identical to the current high-quality version on modern devices while feeling significantly faster, lighter, more stable, and smoother on budget Android phones, old Android phones, older iPhones, weak CPUs, limited RAM, older GPUs, and poor mobile networks. Modern devices should not be slowed down by unnecessary compatibility logic, and older devices should not be overwhelmed by initialization intended for powerful hardware.

The application should ideally behave like this: on a modern device with a good connection, the website opens almost immediately with the complete experience; on an older or weaker device, the essential UI appears first or a polished preparation screen appears only when necessary, critical resources are initialized, the application becomes smoothly interactive, and secondary resources continue loading or caching intelligently in the background; after the first successful visit, core sections become available offline and repeat visits become significantly faster.

Before finishing, verify that there are no broken layouts, missing assets, hydration errors, console errors, API failures, database issues, editing problems, preview problems, navigation regressions, chatbot issues, caching bugs, offline errors, synchronization conflicts, stale service workers, animation glitches, excessive storage use, or memory leaks. Finally, provide a concise technical summary showing what was optimized, which files were changed, which unnecessary code or dependencies were removed, what caching and offline strategies were introduced, what behavior was added specifically for low-end devices, and measurable before-versus-after performance results.